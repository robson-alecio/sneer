package sneer.kernel.communication.impl.tests;



import junit.framework.TestCase;

public class CommunicatorTest extends TestCase {

	public void testXx() throws Exception {
//		BusinessMock business1 = createBusiness(1);
//		BusinessMock business2 = createBusiness(2);
//		
//		OldNetworkMock network = new OldNetworkMock();
//		Communicator communicator1 = createCommunicator(business1.output(), business1, network);
//		Communicator communicator2 = createCommunicator(business2.output(), business2, network);
//
//		business.addAttribute(new ContactAttributesSourceImpl("nick","localhost",port,pk,index).output());
//
//		Connection conn = subject.operator().connectMeWith(new ContactIdImpl(2));
//		assertTrue(conn.isOnline().currentValue());
	}

//	private Communicator createCommunicator(Business business, OldNetworkMock network) {
//		User user = null;
//		return new Communicator(user, network, business, contactManager);
//	}

//	private BusinessMock createBusiness(int index) {
//		int port = 10000 + index;
//		String pk = "PK" + index;
//		return new BusinessMock(pk, port);
//	}
}
