/${user.home}/.sneer
	/code
		/src
		/bin
		/.settings
		.project
		.classpath
	
	/data
====================================================
sneer (git repository)
	code
		src
			sneer.
				bricks.
				foundation. (commons + brickness)
				main. (Sneer, SneerSession)
				functionaltests.
		.project
		.classpath
	
	/data (.gitignore)
	
	installer
	spikes
	snuggin