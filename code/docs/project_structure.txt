/${user.home}/sneer
	/code
		/src
			sneer.
				bricks.
				foundation. (commons + brickness)
				main. (Sneer, SneerSession)
				functionaltests.
		/bin
		/.setting
		.project
		.classpath
	
	/data
	
	
====================================================
sneer (git repository)
	/code (same as above but with /bin in .gitignore)
	
	/data (.gitignore)
	
	/installer
	/spikes
	/snuggin
